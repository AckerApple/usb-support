{
  "name": "controller-helper",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm-run-all --parallel start:webapp start:socket-server",
    "start:cli": "ts-node index",
    "start:socket-server": "nodemon --watch 'server/**/*.ts' --ignore 'server/**/*.spec.ts' --exec 'ts-node' server/index.ts",
    "start:webapp": "cd webapp && npm start",
    "build": "cd webapp && npm run build",
    "save": "git add . && git commit -m \"update\" && git push"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^14.14.17",
    "ack-x": "^1.7.0",
    "node-hid": "^2.1.1",
    "nodemon": "^2.0.6",
    "npm-run-all": "^4.1.5",
    "rxjs": "^6.6.3",
    "usb-detection": "^4.10.0",
    "ws": "^7.4.2"
  },
  "devDependencies": {
    "typescript": "^4.1.3"
  }
}
